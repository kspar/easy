# Test-only dependencies. Kept apart from requirements.txt so that nothing here is installed on an
# executor host — that machine runs untrusted student code, and its dependency list should be the
# shortest one that works.
#
# Dependabot watches requirements.txt; this file is deliberately not pinned as tightly, because a
# pytest release breaking CI is a five-minute fix and a pinned-and-forgotten test runner is not.
-r requirements.txt
pytest>=8,<10
