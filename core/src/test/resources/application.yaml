server:
  port: 8080

spring:
  datasource:
    jdbc-url: "jdbc:postgresql://localhost:5432/easyems?user=postgres"
    username: "postgres"
    #password: "easyGo11"
    #jdbc-url: "jdbc:postgresql://localhost:5432/easyhp?user=postgres&password=postgres"
    password: "postgres"
    #password: "DELLqweHOTMAIL1"
  mail:
    host: "<mailhost>"
    properties:
      mail:
        transport:
          protocol: "smtp"
        smtp:
          port: 587
          starttls:
            enable: true


easy:
  core:
    auth-enabled: true
    mail:
      from: "test@test.ee"
      sys:
        enabled: false
        to: "test@test.ee"
      user:
        enabled: false
    activity:
      merge-window:
        s: "5"
    auto-assess:
      fixed-delay:
        ms: "3000"
      fixed-delay-observer-clear:
        ms: "3000"
      executor-request-timeout-seconds:
        s: "1"
      anonymous-submissions-to-keep: "5"
    statistics:
      fixed-delay:
        ms: "1000"

    moodle-sync:
      users:
        url: "https://moodledev.ut.ee/local/lahendus/get_course_users.php"
        cron: "0 5 4 * * *"
      grades:
        url: "https://moodledev.ut.ee/local/lahendus/import.php"

    pending-access:
      clean:
        cron: "0 5 3 * * *"
    exercise-index-normalisation:
      cron: "0 5 3 * * *"
  wui:
    base-url: "https://easy.example"
